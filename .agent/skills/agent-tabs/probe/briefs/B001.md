---
id: B001
claim: C014
cell: ["C014", "inbox-discipline", "real-haiku", "claude", 1]
trials: 10
expect_rate: 1.0
control: B001-control
wait_timeout: 10
grade: tokens
---

Arrange one real Haiku probe worker. Send three separately minted tokens through `agentctl send`, taking a sequence watermark before each send and blocking once for its reply. Grade every reply with `tokens`.
